//Only for default settings under development, remember too delete sensitive info before sharing or committing to version control.

//SMTP settings for outgoing email, e.g. for notifications and reminders
SMTP-Host: smtp.example.com
SMTP-port: 587
SMTP-User: your-user@example.com
SMTP-Pass: <set-in-local-env>
//Imap settings for incoming email fetching, e.g. for reminders and calendar invites
IMAP-Host: imap.example.com
IMAP-Port: 993
IMAP-User: your-user@example.com
IMAP-Pass: <set-in-local-env>
